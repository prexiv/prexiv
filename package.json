{
  "name": "prexiv",
  "version": "0.1.0",
  "description": "PreXiv — a research manuscript archive with explicit AI-use provenance, hosted artifacts, version history, and optional human audit statements",
  "private": true,
  "scripts": {
    "start": "cd rust && cargo run --release",
    "dev": "cd rust && cargo run",
    "build": "cd rust && cargo build --release",
    "test": "cd rust && cargo test",
    "lint": "cd rust && cargo clippy --all-targets -- -D warnings",
    "fmt:check": "cd rust && cargo fmt --check",
    "test:mcp": "python3 tests/prexiv_mcp_test.py",
    "check": "npm run fmt:check && npm run test && npm run lint && npm run build && (cd mcp && npm run check)"
  }
}
